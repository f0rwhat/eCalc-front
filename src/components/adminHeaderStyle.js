import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    a_fill_div: {
        textAlign: 'center',
        height: '100%',
        width: '100%',
        textDecoration: 'none',
        color:'black'

    },
});
